%p= t('views.devise.mailer.password_change.text', user_name: (@user_name || @email), here: link_to('aquí', edit_password_url(@resource, reset_password_token: @token)))
%p= t('views.devise.mailer.password_reset_intructions.text2')

